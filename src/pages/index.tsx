import * as React from 'react';
import { HomePage } from '../components/home-page';
import { Layout } from '../layout';

const Blog: React.FC = () =>
  <Layout>
    <HomePage />
  </Layout>
  ;

export default Blog;
